# SEO 执行手册：Credit Card Generator

## 用途

这是本项目以后执行 SEO 的唯一进度与任务主流程。以后用户询问“SEO 下一步是什么”“现在做到哪里”或要求继续 SEO 时，先读取本文件的 `当前停止点`，只推进一个尚未完成且前置条件已满足的步骤。完成后立即更新清单和停止点，不需要反复打开俱乐部行动手册。

做优先级判断、解释原因或决定是否采用某种 SEO 做法时，先读取 `SEO_OPERATIONS_METHOD.md`。该文件综合了行动手册、会议《俊哥AI_ SEO优化和站点运营》的智能纪要与文字记录，并使用 Google 官方文档校准了课程中的经验说法。

本流程按 `SEO_OPERATIONS_METHOD.md` 的方法论收窄为适合当前静态工具站、Cloudflare Pages、公开 GitHub 仓库和广告候选商业模式的执行步骤。

## 执行原则

1. 顺序是：搜索意图与关键词 → 页面/技术 SEO → GSC → Analytics → 收录验证 → 外链冷启动 → 数据复盘 → 商业化判断。
2. 每次只处理当前步骤；未经验证不标记完成。
3. 区分一次性建设和持续运营；不要用“发过一次外链”代替长期复盘。
4. 不批量制造近似页面，不堆关键词，不购买或自动生成低质量外链。
5. 不把“绕过免费试用、支付或账户验证”写入页面、文章、标题或推广文案。
6. 暂不接支付；广告也要等真实、合规的自然流量出现后再评估。

## 总流程

```text
0. 上线基线确认
→ 1. 关键词与搜索意图
→ 2. 页面 SEO
→ 3. 技术 SEO
→ 4. Google Search Console
→ 5. Google Analytics
→ 6. 收录与数据基线
→ 7. 外链与曝光冷启动
→ 8. 周期性复盘和迭代
→ 9. 商业化判断
```

## 0. 上线基线确认（一次性）

- [x] 正式域名可通过 HTTPS 访问：`https://creditcardgenerator.online/`
- [x] GitHub `main` 分支与 Cloudflare Pages 自动部署已打通。
- [x] 桌面和手机端关键流程通过。
- [x] 7 种卡组织、1–20 张批量、复制和导出自动验收通过。
- [x] 页面不依赖登录、数据库、API Key 或服务端。

**完成条件**：正式域名可访问，核心功能和响应式布局均无阻塞问题。

## 1. 关键词与搜索意图（先定基线，再做页面）

### 要做什么

1. 确定一个首页主词，不同时追逐多个方向。
2. 记录 5–15 个相关词：Google 自动补全、Google Trends、GSC 后续真实查询词。
3. 查看主词搜索结果前 3–10 名，记录它们的页面类型、功能、标题、内容结构和外链来源。
4. 区分搜索意图：开发/QA、表单验证、随机测试数据，以及无法可靠满足的真实支付或试用验证。
5. 写下本页独特价值，不把“与竞品一样”当作价值：快速、无登录、本地生成、7 种卡型、20 张批量、JSON/CSV、响应式界面。

### 已确认的首页基线

- 首页主词：`random credit card generator`
- 相关词：`random credit card generator`、`credit card number generator`、`test credit card generator`、`Luhn valid card generator`
- 直接搜索意图：立即使用在线工具生成随机卡片信息。测试与免费试用等背后用途比例未知，不在上线前强行二选一。

### 产物

- 更新 `RESEARCH_SUMMARY.md` 中的关键词、搜索意图、竞品和未知项。
- 明确首页 title、description、H1 和正文分别承接什么意图。

**完成条件**：首页主词和页面意图得到确认，且不包含无法兑现或不合规的承诺。

## 2. 页面 SEO（一次性建设，后续按数据迭代）

### 检查清单

- [x] `<title>` 包含主词，并清楚表达页面价值。
- [x] Meta description 与真实功能一致。
- [x] 页面只有一个 H1：`Random Credit Card Generator`。
- [x] H2/H3 层级清楚，How it works 与 FAQ 是静态可抓取文本。
- [x] 增加 canonical，指向正式域名首页。
- [x] 增加 Open Graph / social sharing 基础文字信息。
- [ ] 检查正文是否覆盖核心问题，但不为字数堆砌内容。
- [ ] 如果未来增加图片：使用描述性文件名、Alt 和压缩后的资源。
- [ ] 只有出现真正不同的用户任务时才增加新页面，并使用内部链接连接。

**完成条件**：搜索引擎和用户能从 title、H1、首屏与正文准确理解页面用途；页面没有重复、误导或关键词堆砌。

## 3. 技术 SEO（一次性建设）

### 必做文件与配置

- [ ] 创建根目录 `robots.txt`，允许正常抓取并声明 Sitemap 地址。
- [ ] 创建根目录 `sitemap.xml`，只放正式 canonical URL。
- [ ] 增加 favicon。
- [ ] 增加 Privacy 页面并从页脚链接。
- [ ] 确认正式域名返回 200、HTTPS 正常、没有横向溢出和严重控制台错误。
- [ ] 检查 `pages.dev` 地址与正式域名的重复内容处理：优先重定向，至少保证 canonical 统一。
- [ ] 验证 `robots.txt`、`sitemap.xml`、favicon 和 Privacy 页面在正式域名可访问。

### Codex 负责

- 创建和修改本地文件。
- 运行检查、提交 GitHub、等待 Cloudflare 自动部署并验证线上结果。
- 不在未说明的情况下添加分析、广告或第三方脚本。

**完成条件**：所有技术文件已部署，正式域名是唯一首选地址，搜索引擎可以抓取首页和必要页面。

## 4. Google Search Console（需要用户登录配合）

### 执行顺序

1. 打开 `https://search.google.com/search-console`。
2. 添加 Domain property：`creditcardgenerator.online`。
3. 使用 Cloudflare DNS 完成所有权验证；如果需要新增 TXT 记录，提交前向用户说明具体记录。
4. 在 GSC 提交 `https://creditcardgenerator.online/sitemap.xml`。
5. 使用 URL Inspection 检查首页。
6. 请求首页索引；不要反复提交同一 URL。
7. 检查 Page indexing、HTTPS、Core Web Vitals 等报告是否有阻塞错误。

### 分工

- 用户：登录 Google、完成必要的账号或授权确认。
- Codex：指导每一步、检查页面状态、准备或验证 DNS/站点文件；涉及账号授权和最终提交时遵守确认要求。

**完成条件**：Domain property 已验证，Sitemap 状态正常，首页可在 URL Inspection 中被读取。

## 5. Google Analytics（需要用户登录配合）

### 执行顺序

1. 打开 `https://analytics.google.com/`，创建 GA4 媒体资源和 Web data stream。
2. 域名填写 `https://creditcardgenerator.online/`。
3. 获取 Measurement ID，并将官方脚本加入网站。
4. 部署后在 Realtime 验证访问是否出现。
5. 只记录匿名产品行为，不发送生成的卡号、CVV、姓名或其他敏感内容。

### 候选匿名事件

- `generate_cards`
- `copy_number`
- `copy_json`
- `download_csv`
- `open_feedback`

**完成条件**：Realtime 能看到访问；事件名称和数据字段经过检查，不包含生成结果或个人信息。

## 6. 收录与数据基线（等待型步骤）

### 每周记录

- GSC：展示量、点击量、CTR、平均排名、查询词、国家、收录状态。
- GA：访问、来源、设备、生成行为、导出行为、反馈点击。
- 记录日期范围，不用单日波动下结论。

### 判断规则

- 没有收录：先排查抓取、canonical、robots、Sitemap 和页面质量。
- 有展示无点击：检查排名、title 和 description 是否匹配意图。
- 有点击无使用：检查首屏、速度、移动端和生成流程。
- 有使用无回访：这可能是工具本身的低频属性，不自动判断为失败。

**完成条件**：形成第一份可复核的搜索与使用基线，而不是只看自己能否打开网站。

## 7. 外链与曝光冷启动（持续任务）

### 优先顺序

1. 选择真正适合开发者工具的 2–3 个渠道，不一次铺满目录。
2. 候选：Show HN、Dev.to、Product Hunt、相关开发者社区、合适的工具目录。
3. Dev.to 等转载内容设置 canonical；文章本身要提供使用经验或技术解释，不只是广告。
4. Product Hunt 发布前准备标题、描述、截图和完整可用的网站。
5. 使用 Ahrefs Backlink Checker 研究排名靠前竞品的真实链接来源，再判断哪些可以合理获得。
6. 每次发布记录：平台、URL、日期、内容、是否通过、是否产生访问或链接。

### 禁止做法

- 不批量发无关论坛和评论区。
- 不使用关键词堆砌的锚文本。
- 不自动生成外链。
- MVP 阶段不买付费外链。
- 不冒充普通用户推荐自己的产品。

**完成条件**：获得一批可追踪、与产品相关的真实曝光渠道，而不是只累计“提交次数”。

## 8. 周期性复盘和迭代

### 每周快速检查

- 收录或技术错误。
- 新查询词和展示变化。
- 是否有真实使用或反馈。
- 已提交外链的结果。

### 每月复盘

1. 哪些查询带来展示和点击？
2. 页面是否满足这些查询的意图？
3. 哪些功能被使用，哪些只是我们想象的需求？
4. 是否需要调整文案、标题、内容或功能？
5. 是否值得继续投入，还是停止扩张并保留站点？

所有改动都应记录为：旧状态 → 数据/反馈 → 改动 → 观察窗口 → 结果。

## 9. 商业化判断

### 广告

只有满足以下条件才评估：

- 有持续、合规的自然流量。
- 页面内容不会被解释为帮助欺骗、绕过支付或服务限制。
- 广告不会遮挡生成、复制和下载操作。
- 预计收入值得承担隐私、性能和政策成本。

### 付费

当前不做。只有真实反馈反复要求 API、更大批量、保存模板、团队或其他持续价值时，才重新评估。

## 当前停止点

**当前阶段：阶段 1–3，页面与技术 SEO 基础。**

**下一步唯一优先事项：继续完成 SEO 基础包中的技术文件。**

具体顺序：

1. [x] 确认首页主词和直接搜索意图。
2. [x] 更新 title、description、canonical 和 Open Graph 基础文字信息。
3. [ ] 创建 `robots.txt`、`sitemap.xml`、favicon 和 Privacy 页面。
4. 处理 `pages.dev` 与正式域名的重复地址。
5. 部署并逐项验证线上 URL。

完成这一包后，才进入 GSC；GSC 完成后再接 GA；随后才开始外链冷启动。

## Walkthrough 协作规则

当用户要求继续时：

1. Codex 先说明当前步骤、为什么现在做、完成后会看到什么。
2. Codex 能独立完成的文件、部署和验证直接完成。
3. 需要用户登录、授权、验证码或重要外部提交时，停在准确页面并告诉用户只需要做哪一个动作。
4. 用户完成后，Codex继续验证结果，不让用户自行判断技术状态。
5. 每个步骤完成后更新本文件的复选框和 `当前停止点`。
6. 不跳过前置步骤，也不因为手册列出某项就机械加入不适合本产品的支付、页面或推广渠道。
