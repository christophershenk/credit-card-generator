# MVP 需求文档：Credit Card Generator

## 文档状态

草案 v0.4｜当前阶段：实现与本地验收｜GitHub 已接入，自动验收已通过，待人工视觉确认。

## 1. 产品目标

做一个免费、快速、无需登录的英文网页工具：用户选择卡组织和数量后，立即得到一组**合成测试卡片数据**，用于表单和软件测试。

搜索词机会来自 `random credit card generator`。搜索意图并不只有开发者；但产品不承诺真实支付、账户验证或免费试用可用性。

## 2. 对标与取舍

主要对标：[BetterBugs Credit Card Number Generator](https://www.betterbugs.io/development-tools/credit-card-generator)。

保留：单页、无登录、Card Type、Number of Cards（1–10）、一个 Generate 按钮、可复制的结果卡片和简短说明内容。

不复制：BetterBugs 主产品的 QA 系统、Chrome 扩展、登录、团队协作和其他工具导航；以及“可用于软件免费试用”之类的承诺。

## 3. MVP 用户主流程

```text
打开网页 → 选择 Card Type 和 Number of Cards（1–10）→ 点击 Generate → 查看并复制所需测试卡号
```

## 4. MVP 功能范围

### 输入

- `Card Type` 下拉：Visa、Mastercard、American Express、Discover、JCB、UnionPay、Diners Club。
- `Number of Cards`：整数，范围 1–10，默认 1。
- 一个 `Generate Cards` 按钮。

### 输出

显示所选数量的合成测试卡：品牌、该组织基础格式的卡号、CVV 和未来 1–5 年内的随机有效期；每张提供复制卡号按钮和明确反馈。卡号通过 Luhn 校验，但不关联银行账户、资金或真实持卡人。

### 公开边界

- H1：`Credit Card Generator`。
- 说明：供 development、QA、demos 和 form validation 使用的 synthetic test data。
- 明确：结果不关联银行、资金、真实持卡人或信用额度，不能保证通过真实支付、账户或试用验证。
- 不出现“绕过试用”“无需真实卡获得服务”“可用于付款”等卖点或教程。

## 5. 明确不做

- 不登录、不账号体系、不数据库、不历史记录。
- 不调用 AI、支付、银行卡或第三方数据 API；不需要 API Key 或 Token。
- 不生成真实或可支付的卡，也不检查真实卡余额、有效性或商户接受情况。
- 不提供账单地址、国家、银行、BIN 自定义、金额、PIN、信用卡验证、反欺诈或支付网关模拟。
- 不提供 JSON/CSV/XML/SQL/TXT Output Format、批量导出或开发者 API。
- 不提供姓名、地址、银行、BIN 自定义、导出格式或开发者 API；这些都留待有真实使用反馈后再考虑。
- 不接广告、域名、付费或分析系统，直到 MVP 部署并确认流量后再决定。

## 6. 实现约束

- 第一版是静态、客户端可运行的页面；不上传用户输入或输出。
- 仅产生合成测试数据，使用公开格式规则和 Luhn 校验。
- 视觉和交互参考 BetterBugs 的简洁卡片布局，但不复制其品牌、文案、图片或代码。
- 适配桌面和手机。

## 7. 验收标准

1. 可选择全部 7 种卡组织及数量 1–10，点击按钮稳定生成相应数量的合成测试卡，且不报错。
2. 每张卡号符合所选组织的基础前缀和长度，并通过 Luhn 校验。
3. 每张显示符合该组织规则长度的 CVV 和未来 1–5 年内的有效期。
4. 每张的 Copy 按钮可复制其号码，并显示明确反馈。
5. 数量不是 1–10 的整数时有明确提示。
6. 手机宽度下无需横向滚动即可选择、生成和复制。
7. 页面不暗示可用于真实付款或免费试用。
8. 页面不加载登录、支付、AI 或服务端密钥。

## 8. 当前未知项（不阻塞 MVP）

- 精确词在北美/欧洲的搜索量、国家分布和长期稳定性。
- 非开发者搜索者的比例与回访价值。
- 上线后是否有稳定、合规且值得接广告的流量。
- 后续是否值得增加折叠式“开发者导出”。

## 9. 确认后下一步

完成本地验收后，进入 `PRODUCT_BUILD_WORKFLOW.md` 的阶段 4。后续只有在获得真实使用反馈时，才考虑增加数量、姓名、地址或开发者导出。
